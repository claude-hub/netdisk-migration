{
  "name": "netdisk-migration",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --scope=@claude-hub/netdisk-cli"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
