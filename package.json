{
  "name": "netdisk-migration",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "cli": "lerna run start --scope=@claude-hub/netdisk-cli",
    "web": "lerna run start --scope=@claude-hub/netdisk-web",
    "api": "lerna run start --scope=@claude-hub/netdisk-api"
  },
  "dependencies": {
    "crypto-js": "^4.2.0",
    "dotenv": "^16.4.5",
    "find-yarn-workspace-root": "^2.0.0",
    "progress": "^2.0.3",
    "winston": "^3.13.0",
    "winston-daily-rotate-file": "^5.0.0"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
