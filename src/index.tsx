// Modules
import React from 'react';
import ReactDOM from 'react-dom';

// Constants
import { ROOT_ELEMENT_ID } from 'constants/ui.constants';

// Component
import App from './app';

ReactDOM.render(<App />, document.getElementById(ROOT_ELEMENT_ID));
