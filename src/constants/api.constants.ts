export enum ActionsTransportsE {
  REQUEST = 'REQUEST',
}

export enum HttpActionsTypesE {
  FETCH = 'FETCH',
  SUCCESS = 'SUCCESS',
  FAILURE = 'FAILURE',
}

export enum HttpMethodsE {
  GET = 'get',
  POST = 'post',
  PUT = 'put',
  PATCH = 'patch',
  DELETE = 'delete',
}
